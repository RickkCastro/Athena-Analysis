import ReactLoading from "react-loading";

const Loading = () => (
    <div style={{ marginTop: 30 }}>
        <ReactLoading type={"spin"} color={"#0099FF"} height={50} width={50} />
    </div>
);

export default Loading;
